# Server port for the application
server.port=8080

# MySQL database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Ankita@35
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
# 'create-drop' creates the tables on startup and drops them on shutdown.
# For production, you would use 'validate' or 'none'.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
